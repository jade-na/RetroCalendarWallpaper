{
  // 이 파일을 같은 폴더에 appsettings.Local.json 으로 복사한 뒤 키를 채우세요.
  // appsettings.Local.json 은 .gitignore 에 의해 커밋되지 않습니다.
  // (또는 환경변수 Kasi__ServiceKey 로 설정해도 됩니다.)
  "Kasi": {
    // 공공데이터포털에서 발급받은 일반 인증키(Decoding) 를 넣으세요.
    "ServiceKey": "여기에-발급받은-Decoding-키를-넣으세요"
  }
}
